<h1><%= @artist.name %> Page</h1>
<l1><%= @artist.song_count %> song</li>

<ul>
<% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song.id) %></li>
<% end %>
</ul>


